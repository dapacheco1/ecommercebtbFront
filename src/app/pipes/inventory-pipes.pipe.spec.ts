import { InventoryPipesPipe } from './inventory-pipes.pipe';

describe('InventoryPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
